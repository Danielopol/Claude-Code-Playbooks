---
title: "Claude Export Fixer"
description: "Fix broken line-wrapping in Claude Code exported conversation files — reconstruct tables, paragraphs, paths, and tool calls that were hard-wrapped at fixed column widths, with an automated validation suite."
seoHook: "Exporting a Claude Code conversation to .txt shreds your tables and splits file paths mid-line. This playbook reconstructs the original formatting so the export is actually readable."
targetAudience: "Claude Code users, developers, note-keepers"
exampleUseCase: "\"Fix the broken formatting in this exported conversation\" → A clean version with tables, paragraphs, and paths reconstructed and validated"
category: "productivity"
difficulty: "beginner"
timeToSetup: "5 minutes"
author: "daymade"
sourceUrl: "https://github.com/daymade/claude-code-skills/tree/main/daymade-claude-code/claude-export-txt-better"
tags: ["claude-code", "export", "formatting", "cleanup", "text"]
createdAt: "2026-06-19"
---

## What This Does

Fix broken line-wrapping in Claude Code exported conversation files — reconstruct tables, paragraphs, paths, and tool calls that were hard-wrapped at fixed column widths, with an automated validation suite.


## What's Inside

The template covers:

- **Workflow**
- **What Gets Fixed**
- **Key Design Decisions**
- **Safety**
- **Dependencies**

---

## Quick Start

### Step 1: Create a Project Folder

Make a dedicated folder for this workflow and open it in Claude Code.

### Step 2: Download the Template

Click **Download** above to save the template, then drop it into your project as `CLAUDE.md` (or paste it into your existing one).

### Step 3: Start Working

Tell Claude what you need in plain language — it will follow the template's workflow automatically. For example:

> Fix the broken formatting in this exported conversation

Claude reads the template and runs the steps for you.
