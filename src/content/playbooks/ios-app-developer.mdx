---
title: "iOS App Developer"
description: "Develop iOS/macOS apps with XcodeGen, SwiftUI, and SPM — including Apple Developer signing, notarization, and CI/CD pipelines — and fix the Xcode build failures that block shipping."
seoHook: "Code signing errors, notarization conflicts, and \"Library not loaded @rpath\" can stall an iOS release for days. This playbook encodes the fixes so builds and deploys actually work."
targetAudience: "iOS developers, macOS developers, mobile engineers"
exampleUseCase: "\"My Xcode build fails with code signing error -25294\" → Claude diagnoses the keychain mismatch and walks through the signing and notarization fix"
category: "developer-tools"
difficulty: "advanced"
timeToSetup: "20 minutes"
author: "daymade"
sourceUrl: "https://github.com/daymade/claude-code-skills/tree/main/iOS-APP-developer"
tags: ["ios", "macos", "swiftui", "xcode", "code-signing"]
createdAt: "2026-06-19"
---

## What This Does

Develop iOS/macOS apps with XcodeGen, SwiftUI, and SPM — including Apple Developer signing, notarization, and CI/CD pipelines — and fix the Xcode build failures that block shipping.


## What's Inside

The template covers:

- **Critical Warnings**
- **Quick Reference**
- **XcodeGen Configuration**
- **iOS Version Compatibility**
- **Device Deployment**
- **SPM Dependencies**

---

## Quick Start

### Step 1: Create a Project Folder

Make a dedicated folder for this workflow and open it in Claude Code.

### Step 2: Download the Template

Click **Download** above to save the template, then drop it into your project as `CLAUDE.md` (or paste it into your existing one).

### Step 3: Start Working

Tell Claude what you need in plain language — it will follow the template's workflow automatically. For example:

> My Xcode build fails with code signing error -25294

Claude reads the template and runs the steps for you.
