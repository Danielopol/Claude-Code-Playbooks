---
title: "Windows RDP Connection Doctor"
description: "Diagnose Windows App / AVD / W365 remote-desktop connection quality on macOS — transport selection (UDP Shortpath vs WebSocket), VPN/proxy interference with STUN/TURN, and Shortpath failures from the logs."
seoHook: "Your VDI session is laggy and you can't tell if it's the network or a fallback to slow WebSocket transport. This playbook parses the logs to find why Shortpath failed."
targetAudience: "remote workers, IT support, sysadmins"
exampleUseCase: "\"My Windows 365 session is slow on my Mac\" → Claude checks whether it fell back to WebSocket and finds the VPN/proxy interference blocking UDP Shortpath"
category: "developer-tools"
difficulty: "intermediate"
timeToSetup: "15 minutes"
author: "daymade"
sourceUrl: "https://github.com/daymade/claude-code-skills/tree/main/windows-remote-desktop-connection-doctor"
tags: ["rdp", "remote-desktop", "windows", "macos", "networking"]
createdAt: "2026-06-19"
---

## What This Does

Diagnose Windows App / AVD / W365 remote-desktop connection quality on macOS — transport selection (UDP Shortpath vs WebSocket), VPN/proxy interference with STUN/TURN, and Shortpath failures from the logs.


## What's Inside

The template covers:

- **Background**
- **Diagnostic Workflow**
- **References**

---

## Quick Start

### Step 1: Create a Project Folder

Make a dedicated folder for this workflow and open it in Claude Code.

### Step 2: Download the Template

Click **Download** above to save the template, then drop it into your project as `CLAUDE.md` (or paste it into your existing one).

### Step 3: Start Working

Tell Claude what you need in plain language — it will follow the template's workflow automatically. For example:

> My Windows 365 session is slow on my Mac

Claude reads the template and runs the steps for you.
